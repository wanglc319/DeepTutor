# ============================================
# Sales CRM Postgres — local env template
# ============================================
# Copy to .env.sales and edit before bringing up:
#   cp .env.sales.example .env.sales
#   docker compose -f docker-compose.sales.yml --env-file .env.sales up -d
# ============================================

# ----- Postgres -----
PG_USER=sales
PG_PASSWORD=sales_dev_2026
PG_DATABASE=sales_crm
PG_PORT=5432
PG_TZ=Asia/Shanghai

# ----- pgAdmin (optional: use --profile admin) -----
PGADMIN_EMAIL=admin@sales.local
PGADMIN_PASSWORD=admin_dev_2026
PGADMIN_PORT=5050
